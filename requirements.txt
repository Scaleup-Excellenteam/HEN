# Runtime dependencies.
#
# Versions are constrained to the majors validated in the design review
# (docs/design/2026-08-31-autocomplete-design-review-v2.md ss7): upper bounds keep a
# future major release from silently changing index or serving behaviour.
#
# numpy and pydivsufsort are not imported before M3/M4, but are pinned and
# installed in CI from M0 so that wheel availability in the target environment is
# validated early rather than at integration time (ADR ss7, dependency risk).

PyYAML>=6.0,<7.0
numpy>=1.26,<3.0
pydivsufsort>=0.0.14,<0.1

# Optional web extension. The command line and the Python API work without
# these; they are only needed to serve the browser interface.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
